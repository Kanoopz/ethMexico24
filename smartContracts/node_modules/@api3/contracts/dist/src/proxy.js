"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.computeDapiProxyAddress = computeDapiProxyAddress;
exports.computeDapiProxyWithOevAddress = computeDapiProxyWithOevAddress;
exports.computeDataFeedProxyAddress = computeDataFeedProxyAddress;
exports.computeDataFeedProxyWithOevAddress = computeDataFeedProxyWithOevAddress;
const ethers = __importStar(require("ethers"));
const index_1 = require("./index");
function getDeploymentAddresses(chainId) {
    const api3ServerV1Address = index_1.deploymentAddresses.Api3ServerV1[chainId.toString()];
    if (!api3ServerV1Address) {
        throw new Error(`Api3ServerV1 deployment not found for chain with ID ${chainId}`);
    }
    const proxyFactoryAddress = index_1.deploymentAddresses.ProxyFactory[chainId.toString()];
    if (!proxyFactoryAddress) {
        throw new Error(`ProxyFactory deployment not found for chain with ID ${chainId}`);
    }
    return { api3ServerV1Address, proxyFactoryAddress };
}
function computeDapiProxyAddress(chainId, dapiName, metadata) {
    const { api3ServerV1Address, proxyFactoryAddress } = getDeploymentAddresses(chainId);
    const dapiNameHash = ethers.solidityPackedKeccak256(['bytes32'], [ethers.encodeBytes32String(dapiName)]);
    const initcode = ethers.solidityPacked(['bytes', 'bytes'], [
        index_1.DapiProxy__factory.bytecode,
        ethers.AbiCoder.defaultAbiCoder().encode(['address', 'bytes32'], [api3ServerV1Address, dapiNameHash]),
    ]);
    return ethers.getCreate2Address(proxyFactoryAddress, ethers.keccak256(metadata), ethers.keccak256(initcode));
}
function computeDapiProxyWithOevAddress(chainId, dapiName, oevBeneficiary, metadata) {
    const { api3ServerV1Address, proxyFactoryAddress } = getDeploymentAddresses(chainId);
    const dapiNameHash = ethers.solidityPackedKeccak256(['bytes32'], [ethers.encodeBytes32String(dapiName)]);
    const initcode = ethers.solidityPacked(['bytes', 'bytes'], [
        index_1.DapiProxyWithOev__factory.bytecode,
        ethers.AbiCoder.defaultAbiCoder().encode(['address', 'bytes32', 'address'], [api3ServerV1Address, dapiNameHash, oevBeneficiary]),
    ]);
    return ethers.getCreate2Address(proxyFactoryAddress, ethers.keccak256(metadata), ethers.keccak256(initcode));
}
function computeDataFeedProxyAddress(chainId, dataFeedId, metadata) {
    const { api3ServerV1Address, proxyFactoryAddress } = getDeploymentAddresses(chainId);
    const initcode = ethers.solidityPacked(['bytes', 'bytes'], [
        index_1.DataFeedProxy__factory.bytecode,
        ethers.AbiCoder.defaultAbiCoder().encode(['address', 'bytes32'], [api3ServerV1Address, dataFeedId]),
    ]);
    return ethers.getCreate2Address(proxyFactoryAddress, ethers.keccak256(metadata), ethers.keccak256(initcode));
}
function computeDataFeedProxyWithOevAddress(chainId, dataFeedId, oevBeneficiary, metadata) {
    const { api3ServerV1Address, proxyFactoryAddress } = getDeploymentAddresses(chainId);
    const initcode = ethers.solidityPacked(['bytes', 'bytes'], [
        index_1.DataFeedProxyWithOev__factory.bytecode,
        ethers.AbiCoder.defaultAbiCoder().encode(['address', 'bytes32', 'address'], [api3ServerV1Address, dataFeedId, oevBeneficiary]),
    ]);
    return ethers.getCreate2Address(proxyFactoryAddress, ethers.keccak256(metadata), ethers.keccak256(initcode));
}
//# sourceMappingURL=proxy.js.map