"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.computeApi3MarketAirseekerRegistryAddress = computeApi3MarketAirseekerRegistryAddress;
const ethers = __importStar(require("ethers"));
const index_1 = require("./index");
function getDeploymentAddresses(chainId) {
    const api3MarketAddress = index_1.deploymentAddresses.Api3Market[chainId.toString()];
    if (!api3MarketAddress) {
        throw new Error(`Api3Market deployment not found for chain with ID ${chainId}`);
    }
    const api3ServerV1Address = index_1.deploymentAddresses.Api3ServerV1[chainId.toString()];
    if (!api3ServerV1Address) {
        throw new Error(`Api3ServerV1 deployment not found for chain with ID ${chainId}`);
    }
    return { api3MarketAddress, api3ServerV1Address };
}
function computeApi3MarketAirseekerRegistryAddress(chainId) {
    const { api3MarketAddress, api3ServerV1Address } = getDeploymentAddresses(chainId);
    const initcode = ethers.solidityPacked(['bytes', 'bytes'], [
        index_1.AirseekerRegistry__factory.bytecode,
        ethers.AbiCoder.defaultAbiCoder().encode(['address', 'address'], [api3MarketAddress, api3ServerV1Address]),
    ]);
    return ethers.getCreate2Address(api3MarketAddress, ethers.ZeroHash, ethers.keccak256(initcode));
}
//# sourceMappingURL=market.js.map